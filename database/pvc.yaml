apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:

  name: mongo-volume-claim
  namespace: three-tier
spec: 
  accessModes:  
    - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 10Gi